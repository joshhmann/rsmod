package org.rsmod.content.mechanics.randomevents.scripts

import org.rsmod.plugin.scripts.PluginScript
import org.rsmod.plugin.scripts.ScriptContext

// TODO: Implement F2P random events (Genie, Mysterious Old Man, Sandwich Lady).
// Random events require NPC spawning via the protected-access queue system.
// Stub left in place so the module compiles.
class RandomEventScript : PluginScript() {
    override fun ScriptContext.startup() {}
}
