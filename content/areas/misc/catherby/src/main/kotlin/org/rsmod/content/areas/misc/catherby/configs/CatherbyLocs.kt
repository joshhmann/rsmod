@file:Suppress("unused", "SpellCheckingInspection")

package org.rsmod.content.areas.misc.catherby.configs

import org.rsmod.api.type.refs.loc.LocReferences

typealias catherby_locs = CatherbyLocs

object CatherbyLocs : LocReferences() {
    // TODO: Add location (object/scenery) references
    // val example_loc = find("cache_loc_name")
}
