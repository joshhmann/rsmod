package org.rsmod.content.quests.belowicemountain

import jakarta.inject.Inject
import org.rsmod.plugin.scripts.PluginScript
import org.rsmod.plugin.scripts.ScriptContext

class BelowIceMountain @Inject constructor() : PluginScript() {
    override fun ScriptContext.startup() {
        /**
         * TODO: Implement Below Ice Mountain quest logic.
         * 1. Add start NPC (Willow) dialogue at Ice Mountain.
         * 2. Implement recruitment of the crew (Burntof, Checkal).
         * 3. Implement the ruins exploration in Camdozaal.
         * 4. Implement the Golem combat and puzzle mechanics.
         * 5. Final completion and rewards (unblocking the ruins).
         */
    }
}
